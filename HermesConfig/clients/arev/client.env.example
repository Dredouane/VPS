# HermesConfig — client AREV Travaux (client #1)

# --- Identité (non secret) ---------------------------------------------------
CLIENT_SLUG=arev
CLIENT_NAME="AREV Travaux"

# --- Telegram ----------------------------------------------------------------
# ⚠️ Option A validée : NOUVEAU bot @BotFather pour l'agent pro (l'agent natif
# @Arev_Chantiers_AssistBot reste en parallèle pendant les tests — pas de 409).
# À renseigner après création du bot via @BotFather :
TELEGRAM_BOT_TOKEN=
# User IDs Telegram autorisés (séparés par des virgules)
TELEGRAM_ALLOWED_USERS=

# --- Modèle / fournisseur LLM ------------------------------------------------
HERMES_MODEL_PROVIDER=deepseek
HERMES_MODEL=deepseek-chat
DEEPSEEK_API_KEY=

# --- Optionnel ---------------------------------------------------------------
# HERMES_PORT=8654
# HERMES_IMAGE_TAG=v2026.5.16-522
