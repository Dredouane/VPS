# HermesCapabilities — requirements.txt (D21)
# Extraction documentaire multi-types (xlsx, docx, pptx)
# Installé automatiquement au spinoff par spawn-hermes-pro.sh
openpyxl>=3.1,<4
python-docx>=1.1,<2
python-pptx>=0.6.23,<1
